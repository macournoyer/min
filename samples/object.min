Post = Article clone do:
  initialize = method(title, content):
    @title = title
    @content = content
  
  to_html = method:
    "<h1>#{@title}</h1>" +
    "<p>#{@content}</p>"

post = Post clone(title: "Min", content: "It is the awesome!")

post to_html print
