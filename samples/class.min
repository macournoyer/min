class Post(Article):
  attr :title, :content
  
  def initialize(title, content):
    @title = title
    @content = content
  
  def to_html:
    "<h1>#{@title}</h1>" +
    "<p>#{@content}</p>"

post = Post.new("Min", "It is the awesome!")

print post.to_html
