# Just enough stuff to boot up
Object.vtable.add_method(:def): name, closure |
  closure.bind_to_caller!
  caller.vtable.add_method(name, closure)

Object.def :clone :
  vtable.delegated.allocate

load "boolean"
load "object"
load "array"
